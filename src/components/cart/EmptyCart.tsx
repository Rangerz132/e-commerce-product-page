import React from "react";

const EmptyCart = () => {
  return (
    <div className="flex items-center justify-center py-20 text-neutral-dark-grayish-blue font-bold">
      Your cart is empty
    </div>
  );
};

export default EmptyCart;
